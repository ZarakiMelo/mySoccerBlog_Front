import './Hero.css';
import { Box, Typography } from '@mui/material';

const Hero = () => {
    return (
        <Box className='hero'>
            <Typography variant='h1'>Partages ta passion du ballon rond !</Typography>
        </Box>
    );  
};

export default Hero;
